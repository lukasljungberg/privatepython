# Change
x = 11
y = None
z = -1

# Don't change
name = "USERNAME"
if x <= y:
    print(name if z == True else "Try again!")
# STATIC
# Don't change
name = "USERNAME"
if x <= y:
    print(name if z == True else "Try again!")
